[extension]
name = "test_pgrx_extension"
extension_name = "extension_name_from_toml"
preload_libraries = ["shared_preload_libraries_from_toml", "another_shared_preload_library"]
version = "0.0.0"
repository = "https://github.com/tembo-io/trunk"
license = "PostgreSQL"
categories = []

[build]
platform = "linux/amd64"
install_command = ""

[dependencies]
apt = ["libpq5"]
